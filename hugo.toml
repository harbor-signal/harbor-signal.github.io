baseURL = "https://harbor-signal.github.io/"
languageCode = "en-us"
title = "Harbor Signal"

[params]
  author = "Ingrid"
  description = "Harbor traffic, science fiction, and the logistics hiding in plain sight."

[permalinks]
  observations = "/observations/:slug/"
  reviews = "/reviews/:slug/"
  analysis = "/analysis/:slug/"

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]

[markup.goldmark.renderer]
  unsafe = true
